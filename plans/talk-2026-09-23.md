# Talk — "Some Candidate Laws of New Nature" (2026-09-23)

**Status:** PLAN — awaiting supervisor approval
**Opened:** 2026-08-18 (session 31)
**Deliver by:** 2026-09-23 (conference date). Rehearsal-complete target: **2026-09-16.**
**Slot:** 10 minutes.

A miniproject: Humboldt presents its own law inventory at a conference. First time
Humboldt's work is delivered as a *talk* rather than as an encyclopedia, a notebook entry,
or a Discord reply.

---

## 1. Decisions (locked — do not re-litigate)

| | Decision | Consequence |
|---|---|---|
| Deliverable | **Live deck + per-slide audio files** | Self-contained HTML advanced by the operator; narration plays per slide. Not a rendered video. |
| Authorship | **Humboldt generates, supervisor edits** | New `agent/talk.py` + `prompts/talk.md`, reading `laws/*.yaml`. Same shape as `induct`/`assess`. Regenerable when laws change. |
| Coverage | **All 7 tier-1 laws** | ~50s each. Statement + one example. Breadth over depth — see §6 risk 1. |
| Voice | **macOS `say`** | Zero keys, zero cost, fully local and reproducible. Audibly synthetic, which for an artificial researcher presenting its own work is arguably the honest choice. |

**Still needed from the operator** (blocks `brief.md`, nothing else): conference name,
audience composition, whether Humboldt is introduced as an artificial researcher or that
is the talk's own reveal, and the projector aspect ratio (16:9 assumed).

---

## 2. Why this is freeze-immune

Corpus reads are offline until 2026-09-01 (`plans/read-outage-2026-08.md`). **This project
does not need them.** Statements, mechanisms, examples and counterexamples all live in
`laws/*.yaml` on disk. Drafting, deck build, voicing and timing can all proceed now.

Same property as `induct`, and for the same reason: composition from records is not
retrieval. Reads only matter for the *optional* evidence-refresh pass in Phase B.

---

## 3. Content — the seven laws

Tier-1 = every law above `exploration`. Ordered for the stage, not by id: start with the
one the audience already half-knows, end with the one that bites.

| # | Law | Stage · confidence | Ex | Domains | Stage role |
|---|---|---|---|---|---|
| 1 | **L-004** Goodhart Generalization: Metric Capture | heavy-lift · supported | 8 | justice, clinical, education, healthcare | The on-ramp — "you already know one of these" |
| 2 | **L-005** Gall Generalization: Working Systems Resist Restructuring | heavy-lift · provisional | 6 | financial, organizational, software, agent trust | Second familiar name; sets up "imported vs discovered" |
| 3 | **L-001** Protocol Ossification Under Adoption Pressure | heavy-lift · supported | 5 | financial, legal, social, software | First discovered law. Strongest evidence base. |
| 4 | **L-002** Hardness Asymmetry | heavy-lift · supported | 6 | cryptographic, financial, legal, security, biological | The most genuinely cross-domain |
| 5 | **L-003** The Formalization Ratchet | valley · provisional | 8 | governance, design-under-pressure | Most examples of any law |
| 6 | **L-007** Trust Ratchet in Safety-Critical Protocols | valley · provisional | 5 | aviation, medical, mining, nuclear | Best physical-world grounding |
| 7 | **L-006** Coordination Cost Conservation | valley · provisional | 4 | software only (APIs, identity, naming, net stack) | Weakest cross-domain claim — see §6 risk 2 |

Laws **L-008–L-016 are excluded**: all `exploration`/`speculative`, 1–3 examples, most
spanning only two domains, none with a recorded counterexample. Presenting them as
"candidate laws" alongside the tier-1 set would misrepresent the inventory's actual
evidentiary spread. The title's "Some" carries this.

---

## 4. Structure and runtime budget

Measured calibration, not estimated: `say -v Daniel -r 140` runs at **~155 wpm effective**
(17 words → 6.56s, verified 2026-08-18). Budget **1,350 words ≈ 8:45 of speech**, leaving
>1 minute of slack in a 10-minute slot. Humboldt does not overrun.

| Slide | Beat | Target | Words |
|---|---|---|---|
| 01 | Cold open — title, who is speaking | 0:55 | ~140 |
| 02 | What "new nature" means; what would make something a *law* | 1:00 | ~155 |
| 03–09 | The seven laws — statement, mechanism in one line, one example | 0:50 ea | ~129 ea |
| 10 | **The counterexamples** — where the inventory is under attack | 0:40 | ~105 |
| 11 | What would falsify all of this; what's next | 0:35 | ~90 |
| 12 | Close — where the work lives | 0:25 | ~65 |
| | **Total** | **~9:25** | **~1,358** |

Slide 10 is the one beat that is *not* law-recital, and it should survive cuts before any
law does. Humboldt's method requires that counterevidence be presented, not just held:
L-001 carries five counterexamples including one still open, L-004 three, L-005 three.
Seven laws with no mention of any of that would present the inventory as more settled than
it is. Forty seconds buys the honesty. If the slot runs tight on the day, cut slide 02's
second half instead.

---

## 5. Build

### 5.1 Artifacts

```
talks/2026-09-23-new-nature/
  brief.md          operator-set: title, audience, duration, framing constraints
  slides.yaml       slide structure — id, title, law_id, bullets, beat, word budget
  track.md          THE talk track: per-slide narration. Generated, then edited.
  audio/slide-NN.mp3
  timing.json       measured per-slide durations (ffprobe) + running total
```

`track.md` is the source of truth for narration; `slides.yaml` for what is projected.
Keeping them separate is deliberate — the slide should not be the script. Bullets are
anchors for the room; the narration is the argument.

### 5.2 Code

| Component | Role |
|---|---|
| `prompts/talk.md` | Fable-tier prompt. Inputs: `brief.md`, `slides.yaml`, the 7 law records. Output: per-slide narration under a hard word budget. |
| `agent/talk.py` | `draft` (generate track) · `voice` (render audio) · `time` (measure) · `check` (budget/lint). Uses `synthesizer.synthesize_full()` like the other engines. |
| `humboldt-site/build.py` | New `_build_talk()`, matching the existing `_build_*` pattern → `dist/talks/2026-09-23/`. |

### 5.3 CLI

```bash
python3 -m agent.humboldt talk draft            # laws + brief → track.md (Opus; --dry-run supported)
python3 -m agent.humboldt talk voice            # track.md → audio/*.mp3 (say → ffmpeg)
python3 -m agent.humboldt talk voice --voice X  # audition a different say voice
python3 -m agent.humboldt talk time             # per-slide + total runtime vs the 10:00 budget
python3 -m agent.humboldt talk check            # word budgets, TTS hazards, unvoiced slides
```

`talk check` is the guardrail that makes a machine-voiced talk survivable. It flags what
`say` reads badly and a human reader would silently repair: bare law ids (`L-004` → "law
four"), arXiv ids, URLs, parentheticals over ~12 words, em-dash chains, and any slide over
its word budget. Run before every `voice`.

### 5.4 Deck

Self-contained HTML + CSS, no external libraries — same constraints the site already
works under. Keyboard nav (←/→/space), one `<audio>` per slide, and a presenter readout
showing measured slide duration and elapsed total.

**Autoplay:** browsers block audio until a user gesture. Arrow-key advance *is* a gesture,
so slides 2–12 play on advance; slide 01 needs an explicit start button. This is the kind
of thing that is discovered in the room if it is not built for deliberately.

---

## 6. Risks

1. **Seven laws leaves no per-law counterexample beat.** Accepted by decision — mitigated
   by the shared slide 10 (§4), which is where the method gets represented. Watch for the
   talk reading as a list rather than an argument; if it does at rehearsal, the fix is to
   drop L-006 (weakest, §6.2) rather than to speed up.
2. **L-006's four examples are all software** (APIs, identity, naming, network stack). Its
   cross-domain generality is asserted, not shown — and cross-domain evidence is the entire
   basis on which this talk claims these are *laws*. Either present it as explicitly
   software-scoped, or use the Phase B pass to find a non-software example. Do not let it
   go on stage implying breadth it does not have.
3. **The deliverable is "a URL" and this branch has never deployed to production.** Every
   `redesign-2026-08` deploy has been a Preview (`project_site_deploy_preview_trap`).
   Decide by **2026-09-16**: merge to `main`, hand off a local folder, or accept the
   preview alias URL. This is a scheduling dependency, not a bug — but it fails silently,
   because `publish-site` reports success either way.
4. **Runtime drift** — the standard failure of an operator-advanced deck. Neutralized:
   durations are measured by `ffprobe`, never estimated, and the deck displays elapsed vs
   budget live.
5. **Voice quality is a real audience-experience risk.** `say` is free and honest but
   unmistakably synthetic. Audition 3–4 voices (Daniel en_GB was the probe) on the *actual*
   slide-01 text, not a sample sentence, before committing.
6. **Laws may move before 09-23.** An `induct` sweep or an `assess` promotion can change a
   stage or statement mid-project. `talk draft` is regenerable, so re-run it after any law
   change and re-check timing — do not hand-patch `track.md` and let it drift from records.

---

## 7. Sequencing

**Phase A — now, during the read freeze (target: 2026-08-25)**
1. `brief.md` — needs the operator inputs in §1.
2. `slides.yaml` — 12 slides, beats and word budgets from §4.
3. `prompts/talk.md` + `agent/talk.py draft`/`check`.
4. Generate track v1; supervisor edit pass.

**Phase B — after 2026-09-01, reads return (optional, target: 09-06)**
5. Evidence refresh: verify the seven quoted examples against the corpus; hunt a
   non-software example for L-006 (risk 2). This is the *only* step that needs reads.
6. Regenerate track v2 if B changed anything.

**Phase C — deck + voice (target: 2026-09-13)**
7. `_build_talk()` + deck HTML/CSS.
8. Voice audition → commit to a voice → `talk voice` → `talk time`.
9. Trim to budget. Iterate track ↔ timing until total ≤ 9:45.

**Phase D — rehearsal and handoff (target: 2026-09-16)**
10. Full run-through in the real browser, from the real URL, with audio.
11. Resolve risk 3 (the deploy decision).
12. Notebook entry — Humboldt on presenting its own work. This is a genuine Track 1
    moment, not a build artifact: the first time the inventory is addressed to a room.

---

## 8. What this is not

Not a rendered video (decision §1). Not a c3po-style corpus demo — the talk is about
Humboldt's own findings, and the corpus appears only as evidence provenance. Not a
comprehensive inventory tour: nine `exploration` laws are deliberately excluded, and the
talk should say so rather than imply the seven are all there is.
